# Core deep learning framework
torch>=2.0.0
torchvision>=0.15.0

# Data processing
numpy>=1.21.0
pandas>=1.3.0
Pillow>=8.0.0

# Configuration and utilities
PyYAML>=6.0
tqdm>=4.66.3                # datasets>=4.x requires >=4.66.3

# Scientific computing
scipy>=1.7.0
scikit-learn>=1.0.0

# Text processing (for CLIP tokenizer)
ftfy>=6.0.0
regex>=2021.0.0

# HuggingFace streaming (ImageNet-1k via ILSVRC/imagenet-1k)
datasets>=2.14.0
huggingface_hub>=0.19.0
hf_transfer>=0.1.5          # speeds up parquet downloads when HF_HUB_ENABLE_HF_TRANSFER=1
multiprocess>=0.70.0        # hard dep of datasets; sometimes missed by resolver
pyarrow>=21.0.0             # parquet backend (datasets>=4.x requires >=21)
xxhash>=3.0.0               # hash backend used by datasets
requests>=2.32.2            # datasets>=4.x requires >=2.32.2

# Optional: for notebooks and visualization
matplotlib>=3.5.0
seaborn>=0.11.0

